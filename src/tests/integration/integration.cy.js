/*
did not make an extra component to test integration lol sorry



describe('Notification Component Tests', () => {
    beforeEach(() => {
      cy.visit('/path-to-your-component');  // Adjust URL as needed
    });
  
    it('toggles email notification preference', () => {
      cy.contains('Email: false').click();
      cy.contains('Email: true').should('exist');
      cy.contains('Email: true').click();
      cy.contains('Email: false').should('exist');
    });
  });
  
*/